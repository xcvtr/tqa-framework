[project]
name = "tqa-framework"
version = "0.3.0"
description = "Trading framework — crypto, forex, MOEX"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9",
    "requests>=2.31",
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["tqa_framework*"]
namespaces = false

[tool.ruff]
line-length = 100
target-version = "py311"
